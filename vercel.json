
{
    "builds": [{
        "src": "customers/wsgi.py",
        "use": "@vercel/python",
        "config": { "maxLambdaSize": "30mb", "runtime": "python3.9.6" }
    }],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "customers/wsgi.py"
        }
    ]
}